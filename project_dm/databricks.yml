# This is a Databricks asset bundle definition for project_dm.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: project_dm
  uuid: 683ea7c5-d176-4881-861c-003a1217f761

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1772722130468418.18.azuredatabricks.net
      root_path: /Workspace/Users/duebu.ananyapal@gmail.com/ansh-datamodelling/${bundle.name}/${bundle.target}

    presets:
      # Set dynamic_version: true on all artifacts of type "whl".
      # This makes "bundle deploy" add a timestamp to wheel's version before uploading,
      # new wheel takes over the previous installation even if actual wheel version is unchanged.
      # See https://docs.databricks.com/aws/en/dev-tools/bundles/settings
      artifacts_dynamic_version: true

  prod:
    mode: production
    workspace:
      host: https://adb-4377997726942656.16.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/duebu.ananyapal@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/ansh_datamodelling/${bundle.name}
    permissions:
      - service_principal_name: 8eeb6ab2-1596-4d3a-b6e3-c380fd46b76a
        level: CAN_MANAGE
      
  test:
    mode: production
    workspace:
      host: https://adb-3197241655127391.11.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/duebu.ananyapal@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/ansh_datamodelling/${bundle.name}
    permissions:
      - service_principal_name: da0daa96-4949-463c-b6a6-34c647bae175
        level: CAN_MANAGE
  
